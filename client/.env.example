# Base URL of the API server.
# Leave EMPTY in development: Vite proxies /api to http://localhost:5000.
# In production (Vercel) set it to your Render/Railway URL, e.g.
#   VITE_API_URL=https://moi-management-api.onrender.com
VITE_API_URL=
